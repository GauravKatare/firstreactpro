import React from 'react';


const Post =(props) => {
   
    return (
        <div>
            <p>{props.title}</p>
        </div>
            //<input type="text" onChange={this.props.changes} value={this.props.name}></input>
    );
}

export default Post;